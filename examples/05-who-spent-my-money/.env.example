# ============================================================
# v0.5 新增: 计费链路
# ============================================================

# 默认初始余额 (元). 创建 user 时如果没指定 balance, 用这个兜底.
# 单位与价格表一致, 都是 CNY (本章默认按人民币算; 改成美元只需把价格表系数换掉).
INITIAL_BALANCE_CNY=100

# preConsume 时若客户端没传 max_tokens, 用这个值预估 output 上限.
# 这个值越大, 余额预扣得越多, 但同时能拦截更激进的失控调用.
DEFAULT_MAX_TOKENS=4096

# ============================================================
# v0.4 沿用: 持久层 + 内部 Key 体系
# ============================================================

# 本地 SQLite 数据库文件路径 (相对 example 根目录)
DATABASE_URL=./data/gateway.db

# 管理接口 (admin API) 的主令牌. 创建用户 / 签发 Key / 吊销 Key 都需要它.
# 生产环境必须改成强随机串, 否则任何人都能签发新 Key.
ADMIN_TOKEN=admin-change-me

# ============================================================
# 上游 Key (沿用 v0.3, 与本章新引入的「内部 Key」是两套体系)
# ============================================================

# OpenAI 上游
OPENAI_BASE_URL=https://api.openai.com
OPENAI_API_KEY=sk-replace-me

# DeepSeek 上游 (OpenAI 兼容)
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=sk-replace-me

# Anthropic 上游 (协议差异最大)
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_API_KEY=sk-ant-replace-me
ANTHROPIC_VERSION=2023-06-01

# 服务监听端口
PORT=3000
