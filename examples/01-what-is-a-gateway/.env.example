# 上游 OpenAI 兼容 API 地址，可替换为 DeepSeek、Moonshot 等
# 默认值: https://api.openai.com
OPENAI_BASE_URL=https://api.openai.com

# 上游账号 Key
# 注意: 此 Key 会被服务端持有，v0.1 故意把它写在本地配置里
# 第 4 章会引入「内部 Key 体系」把它收口
OPENAI_API_KEY=sk-replace-me

# 服务监听端口，默认 3000
PORT=3000
